import {createRequire} from "module"
const require= createRequire(import.meta.url);
const bcrypt= require('bcrypt');

export const hashed_result=(password)=>{

}

export const compare_password=(hashedpass)=>{
    
}